#include<bits/stdc++.h>
using namespace std;

int main(){
    list<int> l;

    l.push_back(2);
    l.emplace_back(3);

    l.push_front(5);
    l.emplace_front(4);

    // rest functions same as vector 
    // begin, end, clear, insert, size, swap
}